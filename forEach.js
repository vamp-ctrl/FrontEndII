const frutas = [
    'banana', 'maçã', 'laranja', 'uva', 'pera', 'abacaxi', 'kiwi', 'manga', 'morango', 'melancia'
];

frutas.forEach((fruta, indice) => {
    console.log(`${indice + 1} - ${fruta}`);
});